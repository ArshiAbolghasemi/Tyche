[project]
name = "tyche"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dvc[s3]>=3.67.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.21",
]
